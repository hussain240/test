[project]
name = "pyperformance_bm_nbody_opt"
requires-python = ">=3.8"
dependencies = ["pyperf"]
urls = {repository = "https://github.com/python/pyperformance"}
version = "1.0"

[tool.pyperformance]
name = "nbody_opt"
tags = "math"
